# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 数据库（本地 SQLite 文件，不纳入版本管理）
*.db
*.db-journal
*.db-wal
*.db-shm
data/*.db*

# 项目内部笔记 / 代理记忆（不进仓库）
.workbuddy/

# 环境变量 / 密钥文件（真实密钥不进仓库；.env.example 是模板，需入库）
.env
.env.*.local
!.env.example
*.app_secret
.app_key
.internal_app_key
.internal_app_secret
.open_conversation_id
.cleanup_回收站/

# 生成的报告
reports/*.html

# 系统 / 编辑器
.DS_Store
Thumbs.db
.fc_creds.enc
aliyun-backend/fc_creds.py

# 阿里云 AccessKey（真实凭证，严禁入库）
AccessKey.csv
# 部署包（二进制，不入库；本地仅用于上传 FC）
aliyun-backend/*.zip
