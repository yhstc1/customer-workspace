# ============ 客户管理平台 —— 环境变量示例 ============
# 复制本文件为 .env（.env 已被 .gitignore 忽略，不会进仓库）
#
# 本系统采用「手机号自助注册」登录，无需任何第三方凭证。
# 管理员初始账号：手机号 18607184641 / 密码 123456
# 其他成员在登录页用手机号自助注册，默认密码 123456。
#
# （可选）腾讯地图 WebService Key，用于客户地址 -> 经纬度 地理编码：
# TENCENT_MAP_KEY=你的Key
#
# （可选）钉钉群机器人推送 —— 把注册/审核提醒发到钉钉群
# 在钉钉群「智能群助手」里添加「自定义机器人」，安全设置选「加签」，复制：
#   - Webhook 地址（含 access_token 参数）→ DINGTALK_WEBHOOK_URL
#   - 加签密钥（SEC 开头的一串）→ DINGTALK_SECRET
# DINGTALK_WEBHOOK_URL=https://oapi.dingtalk.com/robot/send?access_token=xxxx
# DINGTALK_SECRET=你的SEC密钥
#
# （可选）ngrok 公网地址变更监控 —— 检测到 ngrok 地址变化即推送新地址到钉钉群。
# 在 run_waitress.bat 中已默认开启；如需关闭，删掉该 bat 里的 ENABLE_NGROK_WATCHDOG=1。
# 轮询间隔默认可改 NGROK_WATCHDOG_INTERVAL，支持 4h / 30m / 60s 或纯数字(秒)，默认 4h。

# （FC 部署用，可选）阿里云 AccessKey —— 供 aliyun-backend/ 下部署脚本读取。
# 脚本已从环境变量读取，禁止把真实 AK 写进任何 .py / .md 再提交。
# 在本机 shell 导出，或填入 .env（.env 已被 gitignore）：
# ALIYUN_AK_ID=你的阿里云AccessKeyId
# ALIYUN_AK_SECRET=你的阿里云AccessKeySecret
# ALIYUN_REGION=cn-hangzhou
# ALIYUN_ACCOUNT_ID=你的阿里云账号ID
# ALIYUN_FC_FUNCTION=crm-api-fc
