# 客户管理平台 · 工作环境迁移步骤清单

> 生成日期：2026-08-23
> 适用：从当前云电脑（Windows）迁移到另一台 Windows 电脑继续开发/运维。
> 当前架构：本地 PC Flask（waitress + SQLite）+ GitHub Pages 前端（H5）+ 阿里云 FC 后端（+ 钉钉 H5 免登）。

---

## 0. 迁移包应包含什么（核验清单）

把整个 `customer-workspace/` 目录打包带走。**下面这些是「运行必需」，缺一不可**：

| 项目 | 路径 | 说明 |
|------|------|------|
| 核心源码 | `app.py` `models.py` `geo_service.py` `report_generator.py` `dingtalk_notify.py` | Flask 后端 |
| 前端（GitHub Pages） | `docs/`（`m.html` + `static/`） | H5 移动端，已 push 到 git |
| 前端（PC 端） | `static/` `templates/` | 桌面浏览器版 |
| 运行数据库 | `data/customers.db` | **真实客户数据，约 144K，必带**（注：清单原写 252K，实测当前为 144K） |
| 密钥文件 | `.env` `.app_key` `.app_secret` `.internal_app_key` `.internal_app_secret` `.open_conversation_id` | **含真实凭证，加密传输，勿提交 git** |
| FC 后端（精简后） | `aliyun-backend/` | `server.py` + 打包脚本 + `crm-api-custom-runtime-v2.zip` + `s.yaml` |
| Python 环境 | `venv/` | 约 221M，Python 3.13.14。**⚠️ 不可直接运行**：`venv/pyvenv.cfg` 的 `home`/`executable` 硬编码了旧用户名路径（`C:\Users\User185984\...`），当前机与新机都会找不到 base 解释器。到新机必须重建（见 1.1 + 2.1）或修正 `pyvenv.cfg`，切勿直接 `.\venv\Scripts\python.exe` |

> **APK 工程 `android-webview-shell/`（原 747M，含内置 JDK/SDK）已于 2026-08-23 删除**——当前前端 H5 已部署到 GitHub Pages、移动端走钉钉 H5 微应用，不再需要本地 APK 打包环境。如需重新打包 APK，需另行准备 Android 工程与 JDK/SDK。
| 工具/存档 | `tools/` `archives/` `scripts/` `tests/` `migration/` | 辅助 |
| 项目记忆/自动化 | `.workbuddy/` | memory + automations，**必带** |
| git 仓库 | `.git/` | 已含前端免登提交 |

**不必带走**（可丢弃）：
- `_cleanup_回收站/`（约 119M，都是重复的 FC 部署沙盒、调试脚本、冗余前端副本，已确认无用）
- `h5-static/`（空目录残留，无害，忽略即可）

⚠️ **密钥安全**：`.env` / `.app_secret` 等含真实密钥。打包用加密压缩（如带密码的 zip/7z），到新机解压后**不要** `git add` 这些文件（已在 `.gitignore` 中）。

---

## 1. 新机环境准备

### 1.1 必装软件
- **Git**（完整安装，勾选「Add to PATH」）—— 本机当初没装 PATH 版导致 `git` 命令找不到，新机务必装 PATH 版，或记住完整路径：
  `C:\Users\<你>\.workbuddy\binaries\PortableGit\versions\1.2.0\mingw64\bin\git.exe`
- **Python 3.13**（若不用自带 `venv/`，需重装并 `pip install -r requirements.txt`；**FC 部署脚本另需阿里云 SDK**，务必再装 `pip install -r aliyun-backend/requirements.txt`）
- **Node.js 22**（前端构建/校验用，可选）

### 1.2 解压迁移包
```
把 customer-workspace/ 解压到新机某处，例如 D:\customer-workspace\
```

---

## 2. 本地 PC 运行恢复（Flask + waitress + SQLite）

### 2.1 启动后端
```powershell
cd D:\customer-workspace
# ① 先确保 venv 可用（自带 venv 的 pyvenv.cfg 路径已失效，二选一）：
#   方案A（推荐，干净）：重建 venv
#     C:\Users\<你>\.workbuddy\binaries\python\versions\3.13.12\python.exe -m venv venv
#     .\venv\Scripts\pip.exe install -r requirements.txt
#     .\venv\Scripts\pip.exe install -r aliyun-backend\requirements.txt
#   方案B（保留已装依赖）：把 venv\pyvenv.cfg 里 home / executable 改成新机 python 路径
#     （如 C:\Users\<你>\.workbuddy\binaries\python\versions\3.13.12\python.exe）

# ② 再启动（venv 正常后）
.\venv\Scripts\python.exe app.py
# 或走 waitress（生产用法）：
.\venv\Scripts\python.exe -m waitress --port=5000 app:app
```
- 默认监听 `127.0.0.1:5000`（或 `0.0.0.0:5000`，看 app.py 配置）。
- 浏览器打开 `http://localhost:5000` 验证 PC 端；`http://localhost:5000/m.html` 验证移动端 H5。

---

## 3. FC 后端（阿里云函数计算）—— 通常无需重部署

> **重要**：FC 函数 `crm-api-fc` 的**环境变量（含钉钉免登凭证 `DING_APP_KEY`/`DING_APP_SECRET` 与 MySQL 连接 `MYSQL_*`）已经在线上配置好**。**存储已切到 MySQL RDS 持久化**（设置 `MYSQL_HOST` 即启用，数据落在阿里云 RDS `crm` 库；未设置时回退 `/tmp` SQLite）。换电脑**不影响线上 FC**，除非你改了 `server.py` / `models.py` 需要重新打包上传。

### 3.1 什么情况下需要重新部署 FC
- 改了 `aliyun-backend/server.py`、`app.py`、`models.py` 等后端逻辑。
- 需要更新钉钉凭证（见 5.2）。

### 3.2 重新打包部署步骤
```powershell
cd D:\customer-workspace\aliyun-backend
# 1. 打包（脚本会包含 server.py + 依赖 + data/customers.db）
.\venv\Scripts\python.exe _pack_custom_runtime.py
# 2. 用官方 SDK 部署/更新函数（函数名 crm-api-fc，custom.debian10，监听 9000）
.\venv\Scripts\python.exe fc_deploy_sdk.py update
```
- 部署包：`crm-api-custom-runtime-v2.zip`（已生成，可直接上传阿里云控制台）。
- FC 监听端口 **9000**（`FC_CUSTOM_PORT` 环境变量）。
- 函数 HTTP 地址：`https://crm-api-fc-<id>.cn-hangzhou.fcapp.run`

### 3.3 仅更新环境变量（不改代码）
```powershell
cd D:\customer-workspace\aliyun-backend
.\venv\Scripts\python.exe fc_update_env_sdk.py   # 读本地 .env 同步到 FC
```
⚠️ FC `UpdateFunction` 的 `environmentVariables` 是**全量替换**——脚本已处理「先 GetFunction 快照再合并」，不要手写只传部分 key，否则会清空 `MYSQL_*`/`JWT_*` 等变量。

---

## 4. 钉钉 H5 免登配置（当前已生效，换机不影响）

- **应用**：钉钉开放平台「企业内部应用」`ding4wznc20eynvxcgu1`（AgentId 4771416455），已配为网页应用。
- **移动端首页**：`https://yhstc1.github.io/customer-workspace/m.html?corpId=$CORPID$`
- **PC 端首页**（需修正）：应为 `https://yhstc1.github.io/customer-workspace/`（不是 `customer-workspace.html`）
- **免登原理**：手机钉钉打开 H5 → `requestAuthCode` 拿 code → 后端 `/api/auth/login` 用 `DING_APP_KEY/SECRET` 换 userId → 匹配 `users.dingtalk_user_id` 种 session。
- admin 手机号 `18607184641` 已绑定钉钉 userId `03683725397487`。

### 验证免登是否真生效（区分「免登成功」vs「旧 cookie 还在」）
1. 手机钉钉清理网页缓存（或移除工作台该应用再重加）。
2. 重新打开 H5：
   - **直接进客户列表** → 免登成功（无旧 cookie 也能进）。
   - **弹登录框** → 之前是旧 cookie 撑着，免登未生效，需查后端 `/api/auth/login` 日志。

---

## 5. 密钥与凭证管理

### 5.1 各密钥用途
| 文件 | 用途 |
|------|------|
| `.env` | 本地 Flask 运行环境变量（数据库路径、JWT、腾讯地图 Key、钉钉 webhook） |
| `.app_key` / `.app_secret` | 钉钉**文件推送机器人**身份（`ding4wznc20eynvxcgu1`），发 APK 到群用 |
| `.internal_app_key` / `.internal_app_secret` | 内部应用备用凭证 |
| `.open_conversation_id` | 目标钉钉群 ID（`cidb0QidCT9QJB9RRSiP0FvOg==`，「Workbuddy连接」群） |

### 5.2 重置/更换钉钉 AppSecret 后
1. 更新本地 `.app_secret`。
2. 更新 FC 函数 `DING_APP_SECRET`（用 `fc_update_env_sdk.py`）。
3. 更新本地 `aliyun-backend/*/.env`（若有）。

⚠️ **安全铁律**：钉钉 AppSecret 切勿在聊天窗口明文发送；泄露后立即去开放平台重置作废。

---

## 6. GitHub 同步（前端 H5）

- 远程：`https://github.com/yhstc1/customer-workspace.git`（私有，main 分支）。
- 凭证存 Windows 凭据管理器（GCM，DPAPI 加密），`git push` 自动用，**不要在 `.git/config` 写明文 token**。
- 本机 `git` 不在 PATH 时，用完整路径：
  ```
  & "C:\Users\<你>\.workbuddy\binaries\PortableGit\versions\1.2.0\mingw64\bin\git.exe" push origin main
  ```
- 沙箱无 TTY 推不动时，用临时 remote + classic PAT（用完即删，见历史记忆）。

---

## 7. 常见问题（本机踩过的坑）

| 现象 | 原因 | 解决 |
|------|------|------|
| `git` 命令找不到 | 没装 PATH 版 Git | 用完整路径或重装勾选 PATH |
| FC 登录 500 | Py3.10 不支持 scrypt 哈希 | 已全改 `pbkdf2:sha256`（记忆） |
| 客户列表空白 | FC 打包漏 `data/customers.db` | 打包脚本已加 db + `/tmp` 可写层 |
| 未登录却看到数据 | 前端 token 判断与后端 cookie 不一致 | 启动先 `/api/me` 判断 |
| 免登接口被拦 | `_require_login` 白名单漏 `/api/auth/login` | 已加白名单 |
| FC 环境变量被清空 | `UpdateFunction` 全量替换 | 用快照合并脚本 |

---

## 8. 迁移后验证清单

- [ ] 本地 `app.py` 启动，PC 端 `localhost:5000` 可登录看数据
- [ ] 移动端 `localhost:5000/m.html` 正常
- [ ] 钉钉手机打开 H5 免登生效（清缓存后验证）
- [ ] FC 后端 `https://crm-api-fc-<id>.cn-hangzhou.fcapp.run/api/healthz` 返回正常
- [ ] GitHub Pages `https://yhstc1.github.io/customer-workspace/m.html` 为最新版
- [ ] 密钥文件齐全且未误提交到 git（`.gitignore` 已覆盖）
- [ ] `_cleanup_回收站/` 确认无用后彻底删除释放空间
