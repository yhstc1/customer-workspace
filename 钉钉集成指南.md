# 钉钉扫码登录 + 本地部署 指南

本指南教你如何为「客户管理工作空间」开启**钉钉扫码登录**：本地电脑跑服务，打开网页后用手机钉钉扫码即可登录，无需公网服务器。

> 不想接入钉钉？没关系。**只要不填 AppKey/AppSecret，系统就是本地免登录模式**，双击 `启动.bat` 直接使用，登录页会提供"直接进入系统"按钮。钉钉登录是可选增强。

---

## 一、工作原理

```
浏览器(本地)                     钉钉开放平台              你的 Flask(本地)
┌────────────┐                 ┌──────────┐            ┌──────────────────┐
│  /login    │  内嵌二维码      │          │            │                  │
│  扫码登录页 │ ───────────────→│  扫码授权 │            │                  │
│            │                 │          │            │                  │
│  手机钉扫码 │←────authCode────│          │            │                  │
│            │                 └──────────┘            │                  │
│  带 authCode 跳转  ─────────────────────────────────→│ /api/dingtalk/   │
│                                                       │   oauth/callback │
│                                                       │ authCode→用户信息 │
│  ←──────────────── 登录成功，跳首页 ──────────────────│  写 session      │
└────────────┘                                         └──────────────────┘
```

采用钉钉新版 **OAuth2 扫码登录**（`login.dingtalk.com/oauth2/auth`）：
1. 登录页内嵌二维码（官方 `DTFrameLogin` 组件）
2. 手机钉钉扫码授权，前端拿到 `authCode`
3. 跳到 `/api/dingtalk/oauth/callback`，后端用 `authCode` 换 `userAccessToken`，再拉取当前登录用户信息，写入 session
4. 之后所有页面/接口都需要登录才能访问（未登录自动跳登录页）

---

## 二、准备钉钉应用（拿 AppKey / AppSecret）

1. 打开 [钉钉开放平台](https://open-dev.dingtalk.com)，用钉钉扫码登录
2. 「应用开发」→「企业内部应用」→ 创建应用（选 **H5 微应用**）
3. 进入应用详情，在「凭证与基础信息」拿到：
   - **AppKey**（Client ID）
   - **AppSecret**（Client Secret）
4. 「安全设置」→「登录 / 重定向 URL（回调域名）」里，**添加回调地址**：
   ```
   http://localhost:5000/api/dingtalk/oauth/callback
   ```
   ⚠️ 必须与 `dingtalk_config.py` 里的 `DINGTALK_LOGIN_REDIRECT` 完全一致（默认就是这个）。
5. 「权限管理」里开通 **个人信息（personnel / openid）** 相关权限，扫码登录 scope 用 `openid` 即可。

---

## 三、配置本项目

### 方式 A：直接改代码（本地部署最简单）

编辑 `dingtalk_config.py`，把 AppKey / AppSecret 填进引号里：

```python
DINGTALK_APP_KEY = os.environ.get("DINGTALK_APP_KEY", "填你的AppKey")
DINGTALK_APP_SECRET = os.environ.get("DINGTALK_APP_SECRET", "填你的AppSecret")
```

### 方式 B：环境变量（更安全，推荐正式环境）

```bash
set DINGTALK_APP_KEY=你的AppKey
set DINGTALK_APP_SECRET=你的AppSecret
```

> AppSecret 是敏感信息，别提交到 Git 仓库。

---

## 四、启动与验证

1. 双击 `启动.bat`（或命令行 `python app.py`）
2. 浏览器打开 http://localhost:5000
3. 未登录会自动跳到 http://localhost:5000/login，页面上显示钉钉二维码
4. 手机钉钉「扫一扫」→ 授权 → 自动登录跳回首页
5. 右上角导航栏「🚪 退出」可退出登录

**自检接口**：访问 http://localhost:5000/api/dingtalk/status ，看到 `"configured": true` 即配置成功。

---

## 五、常见问题

### Q: 二维码不显示 / 加载失败？
- 检查电脑能否访问 `g.alicdn.com`（二维码组件 CDN）。断网环境无法显示二维码。
- 确认 AppKey 填对了。

### Q: 扫码后提示"登录失败"或跳回登录页？
- 回调地址没配对。钉钉后台的「重定向 URL」必须**精确等于** `http://localhost:5000/api/dingtalk/oauth/callback`。
- AppSecret 填错。
- 权限没开 openid。

### Q: 想临时关掉登录，直接用？
- 把 `dingtalk_config.py` 里的 AppKey/AppSecret 留空即可。系统检测到未配置就不启用登录门禁。

### Q: 换台电脑 / 换端口怎么办？
- 端口变了（比如 8000），回调地址和 `APP_BASE_URL` 都要同步改，并在钉钉后台重新登记回调地址。

---

## 六（可选）：消息推送

每日报告生成后推送到钉钉。需要在开放平台开启「工作通知」并配置 AgentId：

```bash
set DINGTALK_AGENT_ID=你的AgentId
```

系统已内置推送接口 `/api/dingtalk/notify`，可在定时任务里调用：

```bash
0 8 * * * cd /path/to/customer-workspace && python -c "
from report_generator import generate_daily_report
from dingtalk_service import send_work_notification
import os
filepath = generate_daily_report()
url = os.environ.get('APP_BASE_URL', '') + '/reports/' + os.path.basename(filepath)
send_work_notification(user_id='你的钉钉UserId', title='📊 今日客户报告已生成', content='点击查看今日报告', url=url)
"
```

---

钉钉在这里只是「身份提供者」——负责扫码登录。应用本身完全跑在你本地，数据也存在本地 `data/customers.db`，不经过钉钉服务器。
