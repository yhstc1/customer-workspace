# 移动云电脑部署指南（Windows）

> 适用：中国移动云电脑（桌面云，Windows 系统）部署「客户管理平台」Flask 应用
> 目标：24h 常驻运行 + 公网可访问（客户/团队用浏览器打开网页）+ 可选微信集成

---

## 0. 前置确认：公网访问（最关键一步）

**基础版移动云电脑默认在私有网络（VPC）里，没有公网 IP，外部打不开你跑在里面的网页。**

要让互联网能访问，必须满足以下全部条件（在移动云控制台操作）：

1. **订购时勾选「高级管理包（V2.0 / 天池资源池）」**（个人版常不支持，建议用政企/企业版账号）。
2. **加购「弹性公网 IP + 带宽」**：在【网络管理】→【带宽管理】→【弹性公网 IP】开通，带宽 2–10240 Mbit/s（小团队 5M 足够，约几十元/月，超出 5M 部分约 36 元/Mbps/月）。
3. **配置安全组放行端口**：在云电脑详情【安全组】添加入站规则，协议 TCP、端口 `5000`、源 `0.0.0.0/0`。

> 若只是你自己远程桌面进去操作、不需要外部访问，可跳过第 2、3 步，直接看第 4 节本地运行即可。

---

## 1. 在云电脑里准备运行环境

1. 安装 **Python 3.12**（官网 python.org 下载 Windows installer），**务必勾选 `Add python.exe to PATH`**。
2. 安装 **Git for Windows**（可选，用于拉取代码；也可以直接把代码压缩包传上去）。
3. 打开 **PowerShell**（或 CMD），验证：
   ```powershell
   python --version      # 应显示 3.12.x
   pip --version
   ```

---

## 2. 获取项目代码

**方式 A：Git 拉取**（若已推到 GitHub / 码云等）
```powershell
cd C:\
git clone <你的仓库地址> customer-workspace
cd customer-workspace
```

**方式 B：本地压缩上传**
1. 在本机把项目文件夹打成 zip；
2. 通过远程桌面复制 / 云盘 / 文件助手传到云电脑；
3. 解压到 `C:\customer-workspace`。

> ⚠️ 不要把 `data/customers.db`（客户数据库）和 `.workbuddy/` 目录传错或覆盖。保留云电脑上的 `data/` 即为线上数据。

---

## 3. 安装依赖

在**项目根目录**执行：

```powershell
# 创建并激活虚拟环境（首次）
python -m venv venv
.\venv\Scripts\activate

# 安装依赖（已含 flask / requests / waitress）
pip install -r requirements.txt
```

- 生产服务器用的是 **waitress**（Windows 兼容，gunicorn 仅 Linux，已从依赖中移除）。
- 若要做**微信集成**，还需按微信 4.x 方案配置 `wechat-cli`（需 Windows 微信客户端 + DbkeyHook）。

---

## 4. 启动服务（手动）

直接双击 `start_windows.bat`，或在命令行运行：

```powershell
.\start_windows.bat
```

脚本会：首次自动建 venv 并装依赖 → 用 waitress 监听 `0.0.0.0:5000` 启动 `app:app`。
启动后浏览器打开 `http://localhost:5000` 即可使用。

- 想换端口：运行前设置环境变量 `PORT`，例如 `set PORT=8080` 后再启动。
- 停止：关闭窗口或 `Ctrl+C`。

---

## 5. 防火墙与公网放行

### 5.1 Windows 防火墙（云电脑内部）
以**管理员**身份运行 PowerShell，放行端口（以 5000 为例）：
```powershell
netsh advfirewall firewall add rule name="CustomerWorkspace" dir=in action=allow protocol=TCP localport=5000
```

### 5.2 移动云安全组（控制台）
在云电脑详情页【安全组】→ 添加规则：
- 协议：TCP
- 端口：`5000`（与启动端口一致）
- 源地址：`0.0.0.0/0`（允许所有外部访问；如需限制可填具体 IP 段）

### 5.3 验证公网访问
在**云电脑之外**的机器/手机浏览器访问：
```
http://<你的弹性公网IP>:5000
```
能打开首页即成功。打不开时按第 9 节排查。

---

## 6. 开机自启（常驻运行）

让服务在云电脑重启后**无需登录自动运行**：

1. **以管理员身份**运行 `install_autostart.bat`；
2. 它会注册**一个**计划任务（SYSTEM / ONSTART）：
   - `cw-waitress`：Flask 生产服务（waitress 监听 `0.0.0.0:5000`）；
3. 云电脑开机即自动拉起应用。

> 手动一键方案 `start_all_headless.bat` 仅适用于有桌面的交互会话，不要用于自启。

日常管理：
```powershell
schtasks /Run   /TN "cw-waitress"   # 启动应用
schtasks /End   /TN "cw-waitress"   # 停止应用
schtasks /Delete /TN "cw-waitress" /F
```

---

## 7. 微信集成（可选）

云电脑是带桌面的 Windows 环境，非常适合跑微信客户端 + 自动化：

1. 在云电脑里安装 **Windows 微信客户端**并登录你的账号；
2. 按项目内的微信 4.x 方案配置 `wechat-cli`：
   - 用 **DbkeyHookUI** 截获微信 dbkey（passphrase）；
   - 通过 PBKDF2-HMAC-SHA512（256000 轮）派生每个库的 enc_key；
   - 配置文件指向微信数据目录。
3. 微信集成进程与 Flask 服务可同时在云电脑常驻。

> 详细步骤见微信 4.x 兼容方案文档。

---

## 8. 日常运维

- **更新代码**：`git pull` 后重启服务（`schtasks /End` + `schtasks /Run`，或重开 `start_windows.bat`）。
- **数据备份**：`data/customers.db` 是 SQLite 数据库，定期复制备份；`reports/` 为生成的报告。
- **查看运行状态**：`schtasks /Query /TN "cw-waitress"`。
- **日志**：当前日志直接输出在 `start_windows.bat` 窗口。如需后台写日志文件，可在 `start_windows.bat` 末尾改为 `python -m waitress ... >> app.log 2>&1`。

---

## 9. 故障排查

| 现象 | 可能原因 / 解决 |
|------|----------------|
| 启动报 `python 不是内部命令` | Python 没勾选 Add to PATH；重装并勾选，或改用 `py -3`。 |
| `pip install` 慢/失败 | 换国内源：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple` |
| 外部浏览器打不开网页 | ① Windows 防火墙未放行端口；② 移动云安全组未放行；③ 弹性公网 IP 未绑定/未开通带宽；④ 服务没在运行。依次排查。 |
| 端口被占用 | 设置 `PORT=8080`（或其他）后重启。 |
| 微信集成失败 | 见微信 4.x 方案：确认 DbkeyHook 截获 dbkey、微信版本为 4.x、数据目录路径正确。 |
| 数据库表不存在 500 | 正常情况下 `app.py` 模块加载即 `init_db()`；若仍报错，手动 `python models.py` 初始化。 |

---

## 附：与旧方案的区别

| 项 | 旧（Linux/Docker/穿透） | 新（移动云电脑 Windows） |
|----|------------------------|--------------------------|
| 生产服务器 | gunicorn | **waitress** |
| 启动脚本 | `start.sh` / `deploy.sh`（bash） | `start_windows.bat` |
| 公网 | cpolar 内网穿透 / Docker 云服务器 | 弹性公网 IP + 安全组（无内网穿透） |
| 常驻 | Docker restart / nohup | 计划任务开机自启 |
| 数据库 | SQLite 挂载卷 | 本地 `data/customers.db` |
